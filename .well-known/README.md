# Commons discovery documents

The root discovery documents mirror the public Commons service metadata. The backend serves its authoritative copies from [`backend/.well-known`](../backend/.well-known); keep public discovery metadata consistent with that source.
