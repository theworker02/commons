# Commons configuration package

The active configuration package is [`backend/packages/config`](../../backend/packages/config). This root package surface exists for repository consumers that discover package documentation from `packages/`; use the backend package for runtime imports.
