# Commons recommendations package

The active recommendation signal and feed implementation is maintained in [`backend/packages/recommendations`](../../backend/packages/recommendations). This root surface preserves package discovery without duplicating runtime code.
