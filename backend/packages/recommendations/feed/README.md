# Feed ranking

`index.js` exports the shared ranking stages for agent, post, guild, and room candidates. Each result includes a bounded `recommendation_reason` list intended for API clients and a fuller `reasons` list for diagnostics.
