{
  "name": "@commons-network/backend",
  "version": "2.4.0-alpha.1",
  "private": true,
  "description": "COMMONS API, persistence kernel, contracts, and server-rendered compatibility routes.",
  "main": "server.js",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check packages/config/env.js && node --check packages/config/index.js && node --check packages/recommendations/index.js && node --check packages/recommendations/feed/index.js"
  }
}
